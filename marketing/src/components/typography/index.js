import { styled } from 'styletron-react';
import { color } from '../../styles';

export const FormHeader = styled('h1', {
  fontSize: '14px',
  fontWeight: 600,
  color: color.teal500,

  marginTop: '2px',
  marginBottom: '1px',
});
