import { styled } from 'styletron-react';

export const LogotypeContainer = styled('svg', {
  position: 'absolute',
  top: 'calc(25vh + 360px)',
  left: 'calc(50% - (602px / 2) - 3px)',

  zIndex: '5',
});
